import { Container, Title, NewPasswordForm } from "../components";

export const NewPasswordPage = () => {
  return (
    <Container>
      <Title text={"Create new Password?"} />
      <NewPasswordForm />
    </Container>
  );
};
